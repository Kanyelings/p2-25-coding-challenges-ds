#SCreate a function to convert a CSV text to a “bi-dimensional” array

import csv
def csv(words):
    items = []
    count = 0
    words = words.split(",")
    for things in text:
        items.insert(count, list(things))
        count += 1
    return values